name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"
      # TODO: run scripts/build-release.sh once implemented, then upload
      # cross-compiled binaries as release assets (e.g. via softprops/action-gh-release)
      - run: echo "TODO: release workflow not yet implemented"
