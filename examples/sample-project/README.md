# Sample Project

This directory will hold a real `drp init` output, committed for reference,
once `drp init` is implemented (Phase 1).

TODO: run `drp init sample-project` here and commit the result once the
generator is functional.
